import styled from "styled-components";

export default styled.header`
  margin: auto;
  width: 70vw;
  display: flex;
  margin-top: 1rem;

  input {
    width: 30vw;

    border: 5px solid var(--color-blue);
  }
  p {
    margin-right: 1rem;
    margin-top: 0.4rem;
  }
`;
